- check get...() , set...() functions
- remove all that c code with tab and #define *MAX -> use Qt
- vim displays 'x,0-1' when positionned on the first char of an empty line : do the same
- layout classes to handle geometry and windows positions, also add a layout manager to save 'profiles' ala konqueror
- nyzis : check event handler(qt one?)
- nyzis : report meta, etc..
- implement open mode
-	replay macros and swapfile ...
-	change QString -> QStringlist in swapfile and macros.cpp
